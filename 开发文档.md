# dsh-history 开发文档

> DeepSeek Harness 非线性会话历史插件——开发指南。
> 本文档解释**为什么这样设计**（走过的弯路与教训）、**反对的设计**、**技术事实**、**最终方案**与**开发路线**。请完整阅读后再动手，不要"想当然"地改成别的形态。

---

## 1. 项目背景与动机

dsh（DeepSeek Harness）是一个**线性聊天工具**：会话从第一个 TURN 线性增长，无法跳回任意历史节点。用户需求：

> 给 dsh 增加**非线性会话历史**：任意 TURN 状态可快照、可回退；回退后继续对话；信息真正从会话记录中消失；不产生新会话、不重启进程。

核心诉求（按优先级）：

1. **信息真正删除**——回退到历史节点后，目标之后的消息/工具调用/配置事件**不在会话记录里**（不是隐藏、不是 shadow）。删除范围 = 会话记录 + 官方日志文件；git 影子历史/备份保留（撤销、审计），安全擦除走可选 purge（阶段 5）
2. **同一会话 id**——不产生新会话、列表不膨胀、来回切换干净
3. **不重启进程**——原地热重载，体验丝滑
4. **git 影子仓库**——快照用主流 checkpoint 形态（零复制、git 原生版本管理）

---

## 2. 走过的弯路（为什么最终这样设计）

这个项目迭代了四轮方案，每一轮被推翻的原因**必须记住**，防止回退：

| 方案 | 形态 | 被推翻的原因（用户原话/结论） |
|---|---|---|
| ① fork 分支 | 官方 `session.fork` 从历史 turn 切出新会话 | **「fork 有问题，会导致会话越来越多，两个版本来回切换非常糟糕」**——会话累积、列表污染 |
| ② marker 软回退 | append 空 `assistant/message` marker + `surfaceOp replace` 撤出消息（SiriLee/dsh-rewind 机制） | **「信息都还在会话记录中」**——log/export 仍含被撤消息，只是表面隐藏，不是真正删除 |
| ③ 冷加载（checkout + 重启） | git checkout 会话文件 → 重启 dsh 从磁盘加载 | **「我不能接受冷加载」**——要重启，体验不可接受 |
| ④ restore 副本 + 归档 | 从 git 快照解析 → `ctx.agents.create` 新会话 → 归档旧会话 | 会话仍是"副本"；familyId 分组、index 记录等**复杂状态**与用户"极简"偏好冲突 |
| ⑤ **最终方案（本文档）** | **物理文件替换 + 热重载**（同会话 id 原地回退），全影子仓库 | —— |

**结论**：任何"产生新会话"或"信息仍留存在记录中"或"需要重启"的方案都被否决。只有"同一会话 id、物理文件被历史版本替换、进程内热重载"满足全部诉求。

### 用户明确讨厌的设计（红线）

- ❌ **fork/分支会话**——会话越来越多
- ❌ **marker/软回退**——信息还在记录里
- ❌ **重启/冷加载**——体验差
- ❌ **index 文件、familyId、映射表**——多余状态，git 历史应该承载一切（"我们完全按照 git 的历史来"）
- ❌ **物理副本目录**（checkpoints/ 复制 zstd + workspace）——"我看好多人开发的 checkpoint 都是使用影子仓库"，零复制
- ❌ **裸仓库 + 分离 work-tree 的复杂度**——能内嵌就内嵌（最终：会话仓库、工作区仓库均为**纯裸仓 + plumbing**（快照遍历 / ls-tree 恢复），无 work-tree）

### 用户偏好（确认过的设计）

- ✅ **git 影子仓库**是唯一事实源（快照写 git、时间线读 git、回退取 git）
- ✅ **零复制**：git 直接跟踪官方会话文件与真实工作区
- ✅ **按会话 id 命名仓库**（会话 id 固定 → 仓库 1:1，无家族概念）
- ✅ **分支 = git refs**：`main`（原始路，永不移动）+ `road-<ts>`（跳转后内容变化才产生的新路）全在 git 内部；跳转本身零 git 动作
- ✅ **回退双 checkbox**：会话 / 工作区可独立勾选
- ✅ **极简**：无 index、无 family、无多余状态

---

## 3. 关键技术事实（研究结论，设计的地基）

以下事实经过源码研究 + 实测验证（dsh 0.1.1-rc.2）：

1. **内存 Session 是权威**：event-sourced、append-only；磁盘 `.zstd` 是 write-behind 记录（`ctx.sessions.flush()` 是 durability 屏障）。
2. **官方无公开销毁 API**：`api.sessions` 无 delete/close；`AgentHandle.dispose` 是创建者专属（api-proxy 丢弃 handle）。**但** TypeScript 的 private 方法编译后是普通属性，**运行时可达**：
   - `ctx.sessions.liveEntryFor(session)` → entry
   - `ctx.sessions.detachEntered(entry)` → 触发 `session/disposed` → coordinator retire 写游标
   - `ctx.agents.store`（内部 Map）+ `ctx.agents.detachEntered(entry)` → 摘除 agent
3. **热重载链已验证**（dsh-session-swap 插件，位于 `E:/deepseek_harness_dev/donzy_plugins/dsh-session-swap`）：
   ```
   flush → detach agent → detach session → 换文件 → agents.resume({resumeSessionId})
   ```
   顺序不可乱。running 状态拒绝。
4. **同会话 id 回退 → header 天然匹配**：快照文件就是本会话自己的历史版本（id/cwd 不变），替换后 `assertStoredIdentity` 直接通过——**无需改写 zstd 帧的 header**（dsh-session-swap 需要改写是因为它替换"别的会话"的文件）。
5. **会话 id 固定**（原地替换不换 id）→ 官方路径固定（`~/.dsh/sessions/<project-key>/<session-id>/session.jsonl.zstd`）→ 影子仓库可直接跟踪。
6. **冷加载（重启）可行但被用户否决**：完整回退文件冷加载成功、无 tornMarker 误判（ws1 实测）——仅作降级链。
7. **zstd 格式**：多帧（checksummed header frame + append frames），事件行含 packed chunk rows（`text-chunks`/`reasoning-chunks`/`tool-call-chunks`）。插件已有解码器（与官方 `decodeStorageRecord` 逐字节一致，实测验证）。
8. **前缀缓存（KV cache）是逐字节前缀匹配**：DeepSeek 服务端以 64 token 为单元、请求边界/公共前缀/固定间隔物化缓存单元；命中要求新请求**完整匹配某个已持久化单元**。dsh 的复用契约（session-persistence README）：*A resumed loop can reuse provider cache only when its reconstructed history, current envelope, and model route match*——所以 resume 必须恢复**信封**（system prompt + tools + route），而不只是消息历史。
9. **dsh 如何为缓存"打标记"**：dsh 不打显式标记（DeepSeek 自动前缀缓存）；它通过确定性的方式**保证字节稳定**——① System prompt：`PromptSection{name, order, text}` 按 order 拼接、重名同层抛错；② Tools：`orderTools` 字典序排序（compareToolNames），未配置 toolOrder 时跨机一致；③ Wire：`serializeMessages`/`serializeRequest` 确定性序列化（tool-result 拆成 role=tool、空文本兜底）；④ 请求头：`canonicalHeader` 归一空值 + `request/header` 折叠 + `deepFreeze` + `markAgentLoopRequest`（来自 agent-loop invariant 的 log-reconstruction desync 校验）；⑤ 会话日志 append-only（surface 只追加，compaction 替换是唯一例外）。
10. **前端三层缓存**（dsh-session-swap 笔记）：常驻 client Session 基线 / 会话作用域记录 / 布局 staged 标记——只刷一层会残留旧视图，需完整的 rebindView 链（或整页刷新兜底）。

---

## 4. 最终方案设计

### 4.1 架构总览

```
┌─ 快照层 ─────────────────────────────────────────────┐
│  TURN.Start / TURN.End / 手动快照                      │
│  工作区 plumbing 快照（先，snap= 归因）               │
│  → flush → 会话文件 hash → 与 base 比对：              │
│          相同→跳过；不同→commit（父=base，挂 ws= 配对）│
│          base=跳转目标 → 新建 road-<ts>；否则推进该 ref│
└──────────────────────────────────────────────────────┘
┌─ 回退层（双 checkbox）───────────────────────────────┐
│  ☑ 会话：git show 历史字节 → flush → detach agent     │
│           → detach session → 原子替换物理文件          │
│           → agents.resume（git 零动作，纯 checkout）   │
│           → 前端重建                                  │
│  ☑ 工作区：ls-tree 恢复历史文件 → 覆盖（先备份）        │
└──────────────────────────────────────────────────────┘
┌─ 时间线层 ──────────────────────────────────────────┐
│  git log 解析 commit message（turn/phase/seq/sessionId）│
└──────────────────────────────────────────────────────┘
```

### 4.2 仓库结构

```
$DSH_HOME/.dsh-history/
├── repos/session-<uuid>.git      # 会话仓库（bare，纯 plumbing 快照，无 work-tree）
│                                 #   快照：hash → 与 base 比对（相同跳过）→ commit-tree（父=base）→ 推进 ref
│                                 #   refs：main=原始路（永不移动）；road-<ts>=跳转后内容变化的新路
├── repos-ws/<project>.git        # 工作区影子仓库（bare，plumbing 遍历快照 + ls-tree 恢复）
│                                 #   提交按 project 隔离、消息带 session=<id> 归因；多个会话共享须加排他锁
└── backups/                      # 回退前备份（会话物理文件 + 工作区当前状态）
```

- **工作区快照同样走 plumbing 遍历**（hash-object/mktree，跳过一切 `.git`、遵循排除规则）：cwd 本身是 git 仓库时 `git add -A` 会把整个项目记成**一个 gitlink 条目（内容全丢）**——plumbing 把嵌套仓库当普通目录，普通目录与仓库目录统一
- 会话仓库按 id 命名（1:1），首次快照 init，之后直接使用
- 会话仓库为纯裸仓：快照经 `hash-object` 直接入库（零拷贝），不依赖 work-tree，互不干扰
- **分支语义全部由 git 承载**：`refs/heads/main`（原始路）+ `refs/heads/road-<ts>`（跳转后内容变化的新路），无任何自建状态
- **无自建 index/映射表**（git 自身的 index/reflog 属 git 内部，不入此列）——git 是唯一事实源

### 4.3 快照

**时机**：
1. `turn/start` 事件（会话开始前，快照含 turn/start 事件本身，实践等同开始前）
2. `turn/end` 事件（AI 修改后）
3. 手动快照（面板「快照」按钮，任意时刻）

**流程**（**工作区先建、会话后建**——不同种 hash 互引会形成循环依赖，见下方契约；会话仓库为纯裸仓，plumbing 显式指定父提交）：
```
0. snap = 本次快照配对 id（如 `turn-<turn>-<phase>-<seq>` 或短 uuid）
1. 工作区仓库（先行；消息带 snap + session 归因，不引用尚未存在的会话提交）：
   遍历 cwd（跳 `.git`、遵循排除规则）→ hash-object/mktree/commit-tree → update-ref
   -m "dsh-history: turn N start|end (seq M) session-<id> snap=<snap>"
2. ctx.sessions.flush(session)                    # durability 屏障
3. 会话仓库（裸仓，无 work-tree）：
   a. 读取官方物理文件字节 → hash-object -w       # 内容直接入库，零拷贝
   b. 与当前 **base** 比对（见下）：文件相同 → 结束，零提交、零 ref 变化
   c. mktree（**固定路径 `session-<id>/session.jsonl.zstd`**，与官方目录布局一致，`git show` 路径稳定）
      → commit-tree -p <base> -m <契约 message>
   d. 落点：base = 跳转目标（尚无 road）→ 新建 `refs/heads/road-<ts>` + 本次提交；
      base = road / main tip → 推进该 ref
```

> **base 语义**（**只有内容真的变了才产生提交**；跳转本身在 git 里零动作）：
> - base 解析：① 进程内的跳转目标（`rewind` 记录）→ ② 最新 `road-<ts>` tip → ③ `main` tip
> - 快照内容与 base 的文件相同 → **什么都不产生**（无提交、无 ref 变化、无分支）——"跳过去看看什么都没做"不留任何痕迹
> - 快照内容与 base 不同 → 提交（父 = base）；若 base 是跳转目标（尚无 road）→ 新建 `road-<ts>` 分支（**main 原路不动**）
> - **main = 原始路，永不移动**；跳转后的新内容只出现在 road 分支上；撤销回退 = 再跳回 main/road 上任意节点
> - 跳转目标仅存于进程内；进程重启丢失后按 git 现有路（最新 road / main）提交——git 仍是唯一事实源，降级一致
> - 若任一侧失败产生孤儿提交（有 snap 无配对），时间线按 snap 孤儿展示，不阻塞后续快照

**commit message 契约**（时间线解析依据，格式必须稳定）：
```
会话侧（后建，引用本次配对的工作区提交）：
  dsh-history: turn N start (seq M) session-<id> snap=<snap> base=<hash> ws=<hash>
  dsh-history: turn N end (seq M) session-<id> snap=<snap> base=<hash> ws=<hash>
  dsh-history: manual snapshot session-<id> snap=<snap> base=<hash> ws=<hash>
工作区侧（先建，归因用）：
  dsh-history: turn N start|end (seq M) session-<id> snap=<snap>
```

> **为什么没有 `sess=`/`ws=` 互相引用**：会话提交先建则 `ws=` 指向的提交尚不存在；工作区先建则 `sess=` 指向的提交尚不存在——同周期互引必然循环。
> 解法：**单向引用**（会话→工作区：`ws=` 指向**本次**配对，因工作区先建而存在）+ **`snap=` 配对键**；时间线/回退定位一律以 `snap=` 或 `ws=` 为准。

### 4.4 回退（双 checkbox）

对话框：**☑ 回退会话（默认勾选） ☐ 回退工作区（默认不勾选）**

**会话回退链路**（顺序契约不可乱）：
```
1. 用户选择时间线行（commit + seq），勾选 session / workspace
2. git --git-dir repos/session-<id>.git show <commit>:session-<id>/session.jsonl.zstd
   （同一会话 id，header 天然匹配，无需改写帧）
3. 备份当前物理文件 → backups/
4. flush（排干写缓冲）
5. detach agent：agents.store.get(sessionId) → agents.detachEntered(entry)
6. detach session：sessions.liveEntryFor(session) → sessions.detachEntered(entry)
   （触发 session/disposed → coordinator retire 写游标）
7. 原子替换官方物理文件（同目录临时文件 + rename，node:fs，不走 subprocess——二进制不能过 stdout）
8. agents.resume({ resumeSessionId }) **并恢复目标记录的信封**（见 4.4.1）
9. 前端重建视图（rebindView 链；异常降级整页刷新）
```

#### 4.4.1 回退后的缓存契约（前缀缓存不因跳转而失效）

**问题**：DeepSeek / Anthropic 前缀缓存（KV cache）要求后续请求与已持久化的前缀单元**逐字节匹配**；dsh 的 resume 会重建请求信封（system prompt + tools + route）。跳转前若直接裸 `agents.resume({ resumeSessionId })`（不挂 preset、不声明路由），重建出的请求前缀与跳转前历史的前缀在 tools/system 第一个字节就分叉——从此缓存永远无法命中（实测：跳转后首轮 `cacheRead = 0` 或断崖式下跌）。

**修复**（rewind.ts 步骤 8）：从目标文件字节中解码两件事，然后以"组合改进 → 裸回退"的顺序尝试 resume：

```ts
facts = decodeTargetFacts(目标字节):
  agentPreset  ← 目标 head 的 session 头字段；若有 `agent-preset/selected` 事件则后者胜出
  route        ← 目标最后一条 `request/header` 的 config（provider/model）

tries[0] = agents.resume({
  resumeSessionId,
  agentOptions: { provider, model },          // 路由与目标一致
  setup: async (agentCtx) => agentPresets.mount(agentCtx, facts.agentPreset),  // 重新挂载 preset = 工具集 + 系统提示段
})
tries[1] = agents.resume({ resumeSessionId, agentOptions: { provider, model } })  // preset 挂载失败时的降级

- tries[0] 成功 → 无告警，缓存契约成立
- tries[0] 失败、tries[1] 成功 → 结果带 `compositionWarning`（面板显示 ⚠）：会话可用但 preset 未恢复
- 两者都失败 → 原 resume-failed 路径（detached=true）
```

**为什么这样够**：dsh 的请求信封 = `canonicalHeader({config, system: renderPrompt, tools: ordered})`——mount preset 恢复了 tools（32 个）与 system（18064 字符，与初始逐字节一致），agentOptions 恢复了路由（provider/model/maxTokens 与目标相同）；消息前缀来自被替换的会话文件（字节级一致）。三段都吻合 → 首轮 `cacheRead > 0`，后续轮次在前缀上继续追加，命中持续。

**边界**：
- 目标无 preset 记录（旧会话或未挂载）→ 只做 route 恢复，不挂载（无警告）
- 目标文件损坏 → decode 失败为空 facts → 裸 resume（不失败，仅缓存退化）
- preset 已删除 / 无法挂载 → 降级 + `compositionWarning`
- 工作区指令（AGENTS.md 等）变化也会改变 system prompt 前缀——恢复工作区（勾选回退工作区）时按目标时间对齐，指令内容一致；不恢复工作区时若指令文件在快照后已变，前缀在系统段即分叉（与目标内容的真实差异一致，属预期行为）

**git 侧零动作**（**跳转 = 纯 checkout**：只把会话文件换成目标内容；不产生标记提交、不移动 main、不建任何 ref）：
1. 跳转目标仅**记录在进程内**（供下一次快照做 diff 比对；进程重启丢失 → 快照退化为沿现有路提交，git 自洽）
2. **新分支（road）只在内容真正变化时产生**——由快照层负责（见 4.3）：内容与跳转目标不同 → `road-<ts>` 分支，父 = 跳转目标；main 原路不动
3. **崩溃语义**：
   - resume 前崩溃 → 磁盘文件 = 目标内容，git 原样（main 未动）；冷加载读到目标文件，进程重启后跳转目标丢失 → 快照沿现有路提交，无歧义
   - resume 后崩溃 → 同上，会话已 live；下次快照行为一致
   - 任何情况下：回退前备份在 `backups/`（手动撤销可用）

**工作区回退链路**：
```
1. 从会话 commit message 解析 `ws=<hash>`（或按 `snap=<snap>` 在 repos-ws 定位；均无则按 commit 时间对齐）
2. 备份当前工作区 → backups/（未提交改动不丢）
3. 恢复：`git ls-tree -r <ws-commit>` 取文件清单 → 按 blob（node:fs 写回 cwd；不走 subprocess stdout）
   （**不执行 git clean**——新增文件保留，避免误删；可选"严格恢复"：删除目标 commit 中不存在的文件）
```

**结果与特性**：
- 同一会话 id 原地回到历史时刻，目标之后的信息**从会话记录与官方日志文件中消失**
- **删除范围**：会话记录 + 官方物理日志文件；git 影子历史与 backups/ 保留（撤销、审计）；安全擦除走可选 purge（阶段 5），purge 后不可恢复
- **回退是双向的**：main（原始路）与 road 分支都保留在图上——可再次跳回「回退前」= 撤销回退
- **跳转零 git 痕迹**：不产生节点/分支/ref；**road 分支仅在内容真的变化后产生**（快照层 diff 驱动，见 4.3）
- running 状态拒绝回退；回退前自动备份，失败可恢复

### 4.5 历史视图（git 图，核心 UI）

一个独立的「视图」页面，把会话快照渲染成**类 git graph 的提交图**（参考 GitLens / git-graph / VS Code Git Graph 插件的视觉形态）：

**视觉规格**：
- **垂直排列**：每个快照 = 一个节点，从上（最旧）到下（最新）纵向排列，行高固定
- **结构线（rail）**：节点左侧用彩色竖线连接父子节点；分叉处用**圆角曲线**横向连接不同 rail（如参考图的粉色/黄色/蓝色曲线）
- **节点样式**：
  - 普通快照：实心圆点（rail 颜色）
  - 当前所在节点（HEAD = 最新 road / main tip）：**同心圆**（空心外环 + 实心内点）高亮
  - 分叉点：跳转后内容变化产生的新路（road 分支），从分叉节点以**圆角曲线**接入新 rail（新路换下一循环色，旧路保持原色）；跳转本身不产生节点
- **多色 rail**：不同路（main 路 / 各 road 分支）用不同颜色循环（蓝/黄/粉/绿…），同一路保持同色
- **行内信息**（节点右侧文本）：TURN N · 阶段(start/end/manual) · 相对时间 · commit 短 hash；hover 显示完整时间与会话 id
- **深色主题**

**布局算法**（git graph lane assignment）：
- 按 commit 拓扑排序（`git log --all --topo-order`），为每个 commit 分配 lane（列）
- 单线性历史 → 单 rail 竖线；内容变化产生的分叉 → 分叉节点接新 rail（新路色），旧路在原 rail 继续
- 每一条路占一个 rail：当前路 = 最新 `road-<ts>` tip 所在路（无 road 时 = main，HEAD 同心圆）；分叉仅在「跳转后内容真的变化」时出现
- 本设计**只有分叉（fork）、没有合并（merge）**——「跳转」= 选节点开新路，不存在两路汇合；`%P` 恒为单个父提交

**交互**：
- 点击节点 → 弹出回退对话框（双 checkbox：会话 / 工作区 + 强确认）
- 当前 HEAD 节点标记随回退实时移动
- 支持滚动加载长历史

**数据源**：`git log --all --topo-order --pretty=format:%H|%P|%s|%ct`（`--all` 使 road 分支可见；%P 父 hash 用于连线），解析 commit message 取 TURN/阶段/seq/sessionId/base。

**实现选型**：
- 优先纯 SVG/Canvas 手绘 rail + 曲线（零依赖，符合插件 bundle 约束——client 只能 `require("react")`，无法引第三方图库）
- rail 连线用 SVG `<path>` 贝塞尔/圆角折线；节点用 `<circle>`
- 参考现成算法：git-graph 的 lane 分配逻辑（可移植思路，不引包）

---

## 5. 开发路线（分阶段实施）

### 阶段 0：环境与基线
- [ ] 确认 dsh 版本（0.1.1-rc.2）、插件加载方式（profile cordis.patch.yml + link）
- [ ] 独立验证端口 3082（**绝不碰主实例 3080**）
- [ ] 参考：`E:/deepseek_harness_dev/donzy_plugins/dsh-session-swap`（重载链实现，可移植）

### 阶段 1：热重载链原型（host 端核心，最高风险先验证）
- [ ] 实现 `detachAndResume(sessionId)`：flush → detach agent/session（内部方法）→ resume
- [ ] 实现「物理文件替换」：git show 取字节 → 备份 → 原子替换 → resume
- [ ] 用测试会话验证：替换为历史快照文件 → 会话内容变化、可继续对话、前端列表正常
- [ ] 验证 running 拒绝、失败回滚（备份恢复）

### 阶段 2：快照改造（会话仓库 + 工作区仓库）
- [x] 会话仓库：`repos/session-<id>.git`（bare，纯 plumbing 快照：hash-object / mktree / commit-tree / update-ref）
- [x] **base 语义 + diff 驱动**：快照与 base（跳转目标 > 最新 road > main）内容相同 → 零动作；不同 → 提交（父=base）；base=跳转目标 → 新建 `road-<ts>` 分支（main 不动）；见 4.3/4.4
- [x] 工作区仓库：`repos-ws/<project>.git`（bare，plumbing 遍历快照；嵌套仓库不落 gitlink；消息带 session 归因 + 排他锁；未变树复用父提交）
- [x] repos-ws 排除规则（`info/exclude`：node_modules、dist、.git 等）
- [x] commit message 契约（`snap=` + `base=` + `ws=` 单向引用）
- [x] 无旧结构遗留（本工作区无旧代码）

### 阶段 3：回退 API + 双 checkbox
- [x] `POST /dsh-history/api/rewind`：{sessionId, commit, restoreWorkspace} → 会话链路（**纯 checkout**：git 零动作）+ 可选工作区链路（ls-tree 恢复）；跳转目标进程内记录
- [x] 时间线 API：`GET /dsh-history/api/timeline?sessionId=`（`git log --all --topo-order --pretty=%H|%P|%s|%ct` 解析，含父 hash 供连线）
- [x] 回退前备份 + 失败回滚

### 阶段 4：前端
- [ ] **历史视图（git 图）**：SVG/Canvas 手绘 rail + 节点，lane 分配算法，HEAD 同心圆高亮，多色分叉曲线（无合并，见 4.5）
- [ ] 节点点击 → 回退对话框（双 checkbox + 强确认）
- [ ] rebindView 链（移植 dsh-session-swap：等 removed/added 帧 → 拆作用域 → 复位阶段 → 重开；降级整页刷新）
- [ ] 手动快照按钮（保留）
- [ ] 深色主题（背景 #1a1a1a，与参考图一致）

### 阶段 5：清理与验证
- [x] 无旧代码遗留（本工作区重新实现，无 marker/jump/restore 旧 API）
- [x] **purge**：清理 road refs / reflog / 不可达对象（gc）+ backups 轮换策略；purge 后不可恢复
- [x] 端到端验证：快照 → 回退（会话/工作区/双选）→ 继续对话（形成 road 分支）→ 再回退（双向）→ 手动快照/去重
- [x] 独立端口全流程验证（主实例已 staging，激活 = 重启 DSH Desktop）

---

## 6. 风险与降级

| 风险 | 应对 |
|---|---|
| 内部 API 依赖（detachEntered/liveEntryFor/store）在 dsh 升级后失效 | 版本锁定（0.1.1-rc.2）；失效时降级链：整页刷新 → 手动重启（冷加载替换后的文件，功能等价、体验降级） |
| 工作区文件大（node_modules 等）导致快照慢/仓库大 | repos-ws 排除规则（阶段 2 必做） |
| 回退期间并发输入 | running 拒绝；detach 后无 agent，窗口极小；前端回退时禁用输入 |
| 替换文件后崩溃 | 原子替换保证文件完整；备份可恢复；下次启动冷加载 |
| 二进制损坏（subprocess 转码） | 文件操作全用 node:fs，不走 subprocess stdout |
| cwd 是 git 仓库（嵌套 .git）→ `git add` 记成 gitlink，工作区内容丢失 | 工作区快照用 plumbing 遍历（跳 `.git`），见 4.2 |
| 历史/备份无限增长 | road refs 保留策略 + gc + purge（已实现） |
| 多会话共享同一 cwd 互相污染 | repos-ws 提交带 `session=` 归因 + 每 project 排他锁；工作区回退影响该 cwd 下所有会话（文档化语义） |

---

## 7. 验证方法

- 独立端口：`dsh --profile web --port 3082`（绝不动 3080）
- 关键用例：
  1. 快照产生：turn start/end/manual → 会话/工作区仓库都有 commit、message 含 `snap=`/`base=`/`ws=`；先建工作区、后建会话；内容与 base 相同 → 零动作（去重）
  2. 会话回退：勾选 session → 历史恢复、目标后信息不在（查 session.history）、同 id、可继续对话；**git 零变化**（main 不动、无新 ref、无新提交）
  3. 工作区回退：勾选 workspace → 文件恢复（ls-tree + node:fs）、未提交改动已备份
  4. 双向回退：回退到 A → 再回退到回退前（B）→ 内容往返正确（两次均为零 git 动作）
  5. 拒绝路径：running 时回退被拒、备份恢复成功
  6. 分支语义：快照 A→B→C（main）→ 跳回 A（git 零变化）→ 继续对话且内容变化 → 新快照 D 落在 `road-<ts>`（父=A）→ `git log --all --topo-order` 出现分叉：A→B→C（main）与 A→D（road）；main 不动；再跳回 C 并变化 → 第二条 road（撤销回退正确）
  7. 崩溃语义：resume 前崩溃 → 磁盘 = 目标内容、git 原样（重做或冷加载皆自洽）；resume 后崩溃 → 同前，无歧义
- 参考实现：dsh-session-swap（重载链，位于 `E:/deepseek_harness_dev/donzy_plugins/dsh-session-swap`）
